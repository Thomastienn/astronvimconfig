"""
Generate random test cases for brute force testing.
Print the generated test cases to standard output.
Only print 1 test case per execution (randomly) since the script already handle multiple test cases.

If problem has t (as input) as the number of test cases, generate t test cases.
"""
import random
random.seed(20)

print(1)
